{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "gh-pages --dotfiles -d dist"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "nuxt": "3.0.0-rc.12",
    "sass": "^1.53.0",
    "sass-loader": "^13.0.2"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.4.6",
    "normalize.css": "^8.0.1",
    "pinia": "^2.0.28"
  },
  "overrides": {
    "vue": "latest"
  }
}
