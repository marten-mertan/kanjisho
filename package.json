{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "git subtree push --prefix dist origin gh-pages "
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.7",
    "sass": "^1.53.0",
    "sass-loader": "^13.0.2"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.4.6",
    "normalize.css": "^8.0.1",
    "pinia": "^2.0.28"
  },
  "overrides": {
    "vue": "latest"
  }
}
